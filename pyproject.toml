[project]
name = "registerapp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "quisiou", email = "marco.casteleiro@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "customtkinter>=6.0.0",
    "pandas>=3.0.5",
    "pyarrow>=25.0.1",
    "tkcalendar>=1.6.1",
]

[project.scripts]
registerapp = "registerapp:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"

[tool.uv]
package = false
